import 'package:flutter/material.dart';

class FavoriteSceen extends StatelessWidget {
  const FavoriteSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Center(
      child: const Text(' Tràn sản phẩm yêu thích '),
    ));
  }
}
